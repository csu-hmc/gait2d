version: 2
build:
  os: "ubuntu-24.04"
  tools:
    python: "miniconda-latest"
sphinx:
  configuration: docs/conf.py
formats: all
conda:
  environment: gait2d-dev.yml
python:
  install:
    - method: setuptools
      path: .
