name: gait2d-dev
channels:
  - conda-forge
dependencies:
  # build
  - cython
  - pip
  - setuptools
  # run
  - numpy
  - pydy
  - python
  - pyyaml
  - sympy
  # examples
  - scipy
  # dev
  - ipython
  - jupyter
  - pytest
