name: gait2d-dev
channels:
  - conda-forge
dependencies:
  # build
  - cython
  - setuptools
  # run
  - numpy
  - pydy
  - python
  - pyyaml
  - sympy
  # examples
  - scipy
  # dev
  - ipython
  - jupyter
  - pytest
